# SynthFrame Wireframe Knowledge Base

## Wireframe JSON Format

All wireframes use this structure:

```json
{
  "id": "layout-unique-id",
  "name": "Descriptive Name",
  "canvas_size": {"width": 1440, "height": 900},
  "background_color": "#ffffff",
  "source_type": "prompt",
  "components": [...]
}
```

## Component Types (UPPERCASE)

| Type | Props | Description |
|------|-------|-------------|
| Type | Props | Description |
|------|-------|-------------|
| navigation-bar | `{links: string[], logo: string}` | Standard top nav |
| sticky-header | `{links: string[]}` | Fixed position header |
| mega-menu | `{categories: [{title, links: []}]}` | Complex dropdown menu |
| simple-header | `{title: string}` | Minimal header |
| hero-banner | `{headline: string, subhead: string, cta: string}` | Main landing section |
| video-hero | `{videoUrl: string, headline: string}` | Hero with background video |
| split-hero | `{headline: string, image: string}` | Text left, image right |
| gradient-hero | `{headline: string, colors: string[]}` | Colorful background hero |
| feature-grid | `{features: [{title, icon, text}]}` | Grid of features |
| icon-features | `{features: [{icon, text}]}` | Icon-heavy list |
| card-features | `{cards: [{title, text, image}]}` | content cards |
| timeline | `{events: [{date, title, text}]}` | Chronological list |
| simple-footer | `{copyright: string}` | Minimal footer |
| multi-column | `{columns: [{title, links: []}]}` | Detailed site map footer |
| social-footer | `{socials: string[], copyright: string}` | Footer with icons |
| newsletter | `{placeholder: string, buttonText: string}` | Email signup section |
| macbook-frame | `{}` | Laptop container frame |
| iphone-frame | `{}` | Mobile container frame |

## Component Structure

Each component has:
```json
{
  "id": "unique-id",
  "type": "NAVBAR",
  "position": {"x": 0, "y": 0},
  "size": {"width": 1440, "height": 64},
  "props": {...},
  "children": [],
  "source": "llm"
}
```

## Canvas Sizes by Device

| Device | Width | Height |
|--------|-------|--------|
| laptop | 1440 | 900 |
| desktop | 1920 | 1080 |
| tablet | 768 | 1024 |
| tablet_landscape | 1024 | 768 |
| phone | 390 | 844 |
| phone_small | 320 | 568 |

## Layout Patterns

**Landing Page**: NAVBAR → HERO → SECTION/CARD grid → FOOTER
**Dashboard**: NAVBAR + SIDEBAR → HEADING → CARD stats → TABLE/CHART → FOOTER
**Mobile App**: NAVBAR → content sections → BOTTOM_NAV
**Login/Signup**: Centered FORM with HEADING
**Profile Page**: NAVBAR → IMAGE (avatar) → HEADING → SECTION → FOOTER

## Rules

1. Component types are lowercase kebab-case (e.g., "navigation-bar", "hero-banner")
2. All positions/sizes are in pixels
3. Components must fit within canvas dimensions
4. Every component needs a unique id
5. source is "llm" for AI-generated, "cv" for sketch-detected

## MCP Tools Available

1. **analyze_sketch**: Upload hand-drawn sketch → returns wireframe JSON
2. **generate_wireframe**: Text description → returns wireframe JSON
3. **update_component**: Instruction + wireframe_id → returns modified wireframe JSON
